# QA Agent Phase 1 Tasks

## 목표

1차 MVP 기능이 깨지지 않도록 테스트와 실행 검증을 관리한다.

## 작업

- 자연어 파서 테스트를 유지한다.
- SQLite 저장소 CRUD 테스트를 유지한다.
- Streamlit 앱 import 검증을 수행한다.
- 브라우저에서 예시 명령 등록 흐름을 확인한다.

## 완료 기준

- `pytest`가 통과한다.
- 앱이 `streamlit run app.py`로 실행된다.
- 예시 명령 등록 결과에 제목, 날짜, 시간이 표시된다.

